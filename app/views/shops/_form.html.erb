<%= form_for @shop do |f| %>
  <%= render 'shared/error_messages' %>
  <%= f.label :japanese_name %>
  <%= f.text_field :japanese_name %>


	<%= select('shop', 'district_id', District.all.collect { |d| [ d.japanese_name, d.id ] }) %>

  <%= f.submit yield(:button_text), class: '' %>

<% end %>
